export type Notification = {
    notificationId: string
    questionId: string
    questionTitle: string
    isRead: boolean
    createdAt: Date
    postUserId: string
    nickname: string
    imageUrl: string
    type: string
}
